# ⚡ دليل البدء السريع - 5 دقائق

## الطريقة الأسهل 🚀

### 1. تحميل المشروع

```bash
git clone https://github.com/abdulrhmanbashniny-jpg/HR_Platform.git
cd HR_Platform
```

### 2. تثبيت المكتبات

```bash
pip install -r requirements.txt
```

### 3. تشغيل التطبيق

```bash
streamlit run app.py
```

### 4. افتح المتصفح

```
http://localhost:8501
```

### 5. جرّب تسجيل الدخول

```
رقم الموظف: 001
```

---

## ✅ هل يعمل؟

إذا ظهر لك التطبيق وتمكنت من تسجيل الدخول، تهانينا! 🎉

**ملاحظة:** النظام حالياً يعمل بالذاكرة فقط. البيانات لن تُحفظ بعد إعادة التشغيل.

---

## 🗄️ تريد قاعدة بيانات دائمة؟

### الخطوة 1: إنشاء حساب Supabase (مجاني)

1. اذهب إلى: https://supabase.com
2. اضغط **"Start your project"**
3. سجّل حساب جديد
4. أنشئ مشروع جديد

### الخطوة 2: احصل على المفاتيح

في Supabase:
```
Settings → API
```

انسخ:
- **Project URL**
- **anon/public key**

### الخطوة 3: حدّث الإعدادات

افتح `config.py` وعدّل:

```python
SUPABASE_URL = "https://xxxxx.supabase.co"  # ⬅️ ضع رابطك
SUPABASE_KEY = "eyJhbGc..."                 # ⬅️ ضع مفتاحك
USE_DATABASE = True                         # ⬅️ فعّل قاعدة البيانات
```

### الخطوة 4: أنشئ الجداول

في Supabase:
```
SQL Editor → New Query
```

انسخ محتوى `database_setup.sql` والصقه واضغط **Run**

### الخطوة 5: اختبر الاتصال

```bash
python scripts/test_connection.py
```

يجب أن ترى:
```
✅ جميع الاختبارات نجحت!
```

---

## 🆘 مشاكل؟

### المشكلة: `ModuleNotFoundError`

```bash
pip install -r requirements.txt
```

### المشكلة: `Connection failed`

تحقق من:
- ✅ الإعدادات في `config.py`
- ✅ الاتصال بالإنترنت
- ✅ صحة المفاتيح من Supabase

### المشكلة: الجداول غير موجودة

```bash
# شغّل database_setup.sql في Supabase
```

### المشكلة: المنفذ مستخدم

```bash
streamlit run app.py --server.port 8502
```

---

## 📚 المزيد من التفاصيل

- [دليل كامل](README.md)
- [دليل المستخدم](docs/USER_GUIDE.md)
- [استكشاف الأخطاء](#)

---

## 💡 نصائح

1. ✅ استخدم Python 3.8 أو أحدث
2. ✅ فعّل البيئة الافتراضية (venv)
3. ✅ تأكد من الاتصال بالإنترنت عند استخدام Supabase
4. ✅ احفظ ملف `config.py` في مكان آمن

---

**هل واجهتك مشكلة؟ [أبلغ عنها](https://github.com/abdulrhmanbashniny-jpg/HR_Platform/issues)**

**بالتوفيق! 🚀**
